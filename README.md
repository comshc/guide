To run locally (for testing, &c), install [`cargo`](https://github.com/rust-lang/cargo) and run:

```
cargo install mdbook
```

Then run:

```
mdbook serve --open
```
 Make sure `.cargo/bin` is in your `$PATH`.
